# Example environment configuration
PORT=8080
# Generate a strong secret in production, e.g., using openssl rand -hex 32
JWT_SECRET=change-this-in-production
# Stripe (optional for local dev)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
# SMTP (optional; if not set, emails are logged to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
MAIL_FROM=Smart Hostel cno-reply@smarthostel.locale
# Optional DB path override (useful for tests)
DB_PATH=
